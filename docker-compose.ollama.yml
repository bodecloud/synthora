# Optional overlay when starting with `--profile ollama`.
# Usage:
#   docker compose --profile ollama -f docker-compose.yml -f docker-compose.ollama.yml up -d
services:
  api:
    environment:
      OLLAMA_BASE_URL: http://ollama:11434
      SYNTHORA_EMBEDDINGS: ollama
  worker:
    environment:
      OLLAMA_BASE_URL: http://ollama:11434
      SYNTHORA_EMBEDDINGS: ollama
